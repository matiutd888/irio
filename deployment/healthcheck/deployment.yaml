apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcheck-app
  labels:
    app: healthcheck
spec:
  replicas: 3
  selector:
    matchLabels:
      app: healthcheck
  template:
    metadata:
      labels:
        app: healthcheck
    spec:
      containers:
      - name: healthcheck-container
        # resources:
        #     requests:
        #       cpu: "500m"
        #     limits:
        #       cpu: "500m"
        image: us-central1-docker.pkg.dev/semiotic-mender-401510/alerting-platform/healthcheck:v1
        command: ["/app/target/release/healthcheck"]
        # command: ["sleep", "infinity"]
        envFrom:
            - configMapRef:
                name: postgres-config
